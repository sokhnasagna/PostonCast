<%= form_with(model: lecture, local: true) do |form| %>
  <% if lecture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lecture.errors.count, "error") %> prohibited this lecture from being saved:</h2>

      <ul>
      <% lecture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id, id: :lecture_id %>
  </div>

  <div class="field">
    <%= form.label :id_user %>
    <%= form.text_field :id_user, id: :lecture_id_user %>
  </div>

  <div class="field">
    <%= form.label :id_podcast %>
    <%= form.text_field :id_podcast, id: :lecture_id_podcast %>
  </div>

  <div class="field">
    <%= form.label :enLecture %>
    <%= form.check_box :enLecture, id: :lecture_enLecture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
